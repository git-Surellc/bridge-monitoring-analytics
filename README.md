# 结构监测数据分析系统 (Structure Monitoring Analytics System)

这是一个基于 React 和 Vite 构建的现代化结构监测数据分析平台。该系统旨在帮助工程师快速处理结构（如桥梁、隧道、边坡等）传感器数据，可视化时程曲线，并自动生成分析报告。

## 📅 更新日志 (Changelog)

### v1.3.9 (2026-03-05 v20)
*   **✨ 新功能**：结构排序和分组支持 **Q/S 前缀**。用户在定义 ID 顺序或分组时，可使用 `Q+ID`（如 Q101）表示结构/桥梁，使用 `S+ID`（如 S101）表示隧道。系统会自动识别并匹配对应类型的结构，彻底解决了不同类型结构 ID 重复导致无法区分的问题。
*   **📝 文档**：更新了 API 导入界面的操作提示，增加了关于 Q/S 前缀的使用说明。

### v1.3.8 (2026-03-05 v19)
*   **✨ 新功能**：实现用户定义的结构分组（路段-based）和排序功能。用户可以在数据导入页面上传 Excel 时，指定结构 ID 的顺序（逗号分隔）以及分组规则（如“路段A:1,2,3;路段B:4,5”）。
*   **📊 报告优化**：Word 报告生成引擎重构，支持按用户定义的分组进行章节渲染。每个分组将作为独立的二级标题显示，分组内的结构依次排列，大幅提升了报告的组织性和可读性。
*   **🐛 修复**：解决了 Word 报告中分组渲染的逻辑问题，确保分组名称和结构内容正确嵌套。
*   **⚙️ 系统优化**：增强了 Excel 结构名称的优先级，现在系统会优先使用用户上传的 Excel 文件中的名称覆盖 API 返回的默认名称，满足个性化命名需求。

### v1.3.7 (2026-03-03 v18)
*   **📈 图表预览**：前端页面的时程曲线（Recharts）现已显示单位，并与后端导出保持一致（倾角：°，振动：mg，位移/挠度：mm，裂缝：mm）。
*   **💄 样式优化**：移除页面预览图例；新增 Y 轴“单位 (xx)”标签并调整边距，避免被裁切；图表标题包含单位说明。

### v1.3.6 (2026-03-03 v17)
*   **📈 图表改进**：时程曲线自动显示单位（倾角：°，振动：mg，位移/挠度：mm，裂缝：mm），不同设备按类型匹配单位。
*   **💄 样式优化**：去掉时程曲线图例，提升可读性。Word 报告中的图表同步更新。

### v1.3.5 (2026-03-03 v16)
*   **🐛 修复**：解决了在 Linux/阿里云 环境下生成图片时中文字体缺失导致的乱码问题。系统现在会自动尝试注册常见的中文字体路径（如文泉驿正黑、微米黑等），确保图表中的中文能正常显示。

### v1.3.4 (2026-03-03 v15)
*   **🐛 修复**：修复生成 Word 报告时因 ECharts 实例未销毁导致的内存泄漏问题，解决了在阿里云等云服务器上生成报告卡住（65%）或崩溃的现象。
*   **⚙️ 稳定性**：服务端新增启动时自动清理僵尸任务的逻辑，若服务意外重启，之前的“进行中”任务会被标记为失败，避免前端永久卡顿。

### v1.3.3 (2026-03-03 v14)
*   **🧠 AI 兼容性**：补齐阿里云百炼 Coding Plan 专属端点支持（`https://coding.dashscope.aliyuncs.com/v1`），自动拼接 `chat/completions` 路径。
*   **🐛 修复**：修复 AI 批量任务状态更新的 SQLite 语法问题，解决“前端一直转圈但后端实际已报错/卡住”的现象。
*   **⚙️ 稳定性**：AI 请求增加超时保护并按队列串行执行（一次一个），避免接口不支持并发导致失败。

### v1.3.2 (2026-03-03 v13)
*   **⚡ 性能优化**：彻底重构 AI 分析模块，将批量分析任务移至后端队列执行，支持断点续传和多任务并发，彻底解决了切换页面导致分析中断的问题。
*   **🐛 修复**：优化 Word 报告生成时的设备状态同步逻辑，采用并行请求机制，解决了生成报告时卡在“正在同步设备状态”的问题。

### v1.3.1 (2026-03-03 v12)
*   **🧠 智能模型配置**：AI 配置页新增“模型名称”自定义功能，完美支持阿里云百炼（Coding 专属节点）及其他 OpenAI 兼容接口，解决了 `model not supported` 报错问题。
*   **🧭 导航增强**：恢复了左侧的“结构快速导航”面板，支持一键定位到具体结构并自动展开详情，大幅提升长报告的阅读体验。
*   **🤖 智能总结**：在“评估结论及建议”章节新增 AI 智能总结按钮，可自动生成包含结构数量、传感器状态、风险评估的项目级总结。
*   **💄 UI 优化**：修复了分析工具栏的层级问题 (z-index)，确保其始终悬浮在结构标题栏之上；优化了锚点跳转的滚动偏移量。
*   **🐛 修复**：增加了对 API 配置参数的自动修剪 (trim) 处理，防止因复制粘贴空格导致的连接失败。

### v1.3.0 (2026-03-02 v11)
*   **🧠 智能分析**：新增“算法分析模块”，支持对结构物监测数据进行多维度深度分析。
*   **🔧 分析开关**：引入分析功能开关体系，支持全局开启/关闭，以及按需开启倾角、沉降、加速度、温度、裂缝等专项分析。
*   **🔗 联动分析**：新增“温度-变形”联动分析功能，自动计算皮尔逊相关系数，揭示环境温度对结构变形的影响。
*   **🤖 AI 诊断**：集成 AI 深度分析功能，支持配置自定义 AI 接口，一键生成结构健康状况诊断报告。
*   **📈 专项算法**：内置多种专业分析算法，包括数据质量分析（3σ异常识别）、趋势分析（线性回归）、周期性分析（FFT）、PGA/自振频率计算、裂缝发展预测等。
*   **💄 UI**：分析结果以卡片形式直观展示，支持关键指标高亮和异常预警，并优化了打印适配。

### v1.2.11 (2026-03-02 v9)
*   **💄 UI**：调整数据导入页面的布局，将使用说明移动至左侧空白区域，避免用户需滚动到底部才能看到。
*   **🐛 修复**：修复点击“加载结果并分析”时出现的 `ReferenceError: cn is not defined` 报错。

### v1.2.10 (2026-03-02 v8)
*   **🔒 安全**：实现登录 Token 24小时过期机制，增强系统安全性。
*   **📚 导航**：优化章节导航栏宽度，解决长标题截断问题，提升阅读体验。
*   **📖 指南**：在数据导入页面新增图文并茂的详细使用说明，包含 Excel 模板示例和操作步骤。
*   **✨ 品牌**：全面去除了"桥梁"特定称谓，升级为"结构监测数据分析系统"，支持桥梁、隧道、边坡等多种结构类型。
*   **✨ 新功能**：系统新增登录界面，采用炫酷的星云粒子动画背景，增强科技感。需使用管理员账号登录后方可访问。
*   **📂 文件管理**：新增文件批量管理功能，支持多选文件后一键批量删除，大幅提升管理效率。
*   **💄 UI**：优化版本号显示样式，增加 "By Surellc" 专属署名标识。
*   **🚀 性能优化**：解决大量结构数据加载时的页面卡顿问题。默认不再自动绘制所有时程曲线，改为按需点击绘制。

### v1.2.9 (2026-03-02 v7)
*   **🐛 修复**：彻底重构设备类型识别逻辑，完全基于 API 返回的 `pointTypeName` 字段进行统计，解决设备类型识别不准确的问题。
*   **📊 报表**：前端“设备在线情况”表格和 Word 报告均实现动态列生成，自动适应 API 返回的所有设备类型。
*   **⚡ 优化**：后端统计逻辑优化，移除旧的 Excel Sheet 名称依赖，确保数据源的一致性。

### v1.2.8 (2026-03-02 v6)
*   **✨ 新功能**：使用 Excel 的 Sheet 名称作为设备类型分类依据，使设备类型统计更加准确和灵活。
*   **📊 报表**：分析报表中的“设备在线情况”表格现在会根据 Excel Sheet 名称动态生成列，展示各类型设备的在线率。
*   **🐛 修复**：修复文件管理界面中下载文件时提示“没有文件”的问题（增加静态文件服务及路径回退机制）。
*   **💄 UI**：优化“生成Word报告”按钮的响应速度，点击后立即反馈，避免用户重复点击。
*   **📝 报告**：生成的 Word 报告中新增“分析摘要”章节，与网页预览保持一致。

### v1.2.7 (2026-03-02 v5)
*   **🔒 安全**：升级 API 认证机制，支持用户名+密码加密获取 Token，提升接口安全性。
*   **🔌 集成**：新增设备在线状态实时获取功能，对接平台 `/sensorList` 接口。
*   **✨ 新功能**：
    *   API 导入界面新增用户名输入框。
    *   分析报表页面支持批量刷新设备在线状态。
*   **🐛 修复**：修复设备状态获取接口请求方式错误 (GET -> POST)。

### v1.2.6 (2026-03-02 v4)
*   **🚀 性能优化**：重构 Word 报告生成逻辑，将其从前端迁移至后端。
*   **✨ 新功能**：支持后端异步生成 Word 报告，彻底解决大数据量下前端页面卡顿问题。
*   **📊 图表**：后端集成 ECharts 和 Canvas，实现高质量图表服务端渲染。
*   **🔄 体验**：新增报告生成进度条，实时反馈后台处理状态。

### v1.2.5 (2026-03-02 v3)
*   **🐛 修复**：修正数据库重复文件判断逻辑，增加“结构类型”作为唯一性校验条件，解决相同 ID 但不同结构类型（如桥梁/隧道）被误判为重复的问题。
*   **🔄 优化**：移除 API 导入任务完成后的自动跳转逻辑，避免误操作，现在需要手动点击“加载结果并分析”。
*   **✨ 新功能**：增强数据库文件管理功能：
    *   新增 **文件下载** 按钮。
    *   新增 **清空数据库** 按钮（慎用）。
    *   新增 **筛选功能**（支持按月份和结构名称/ID筛选）。

### v1.2.4 (2026-03-02 v2)
*   **🐛 修复**：彻底修复“加载结果并分析”按钮点击无反应的问题（后端日志补充下载链接）。
*   **💄 UI**：为“加载结果并分析”按钮添加加载中状态动画，提升用户体验。
*   **💄 UI**：更新浏览器标签页图标 (Favicon) 和系统标题。

### v1.2.3 (2026-03-02 v1)
*   **🐛 修复**：修复历史已完成任务无法查看数据的问题。现在 API 导入界面在检测到已完成的任务时，会显示“加载结果并分析”按钮，点击后可手动加载数据并跳转至分析报表。
*   **💄 UI**：优化 API 导入界面的按钮布局。

### v1.2.2 (2026-03-02 v1)
*   **🐛 修复**：修复 API 批量导入界面选择月份时意外跳转至分析页面的 Bug。
*   **🔄 优化**：API 导入界面的月份选择现在会持久化保存，刷新页面后自动恢复任务进度。
*   **💄 UI**：更新系统名称为“结构监测数据分析系统”。
*   **✨ 新功能**：添加网站图标 (Favicon)。

### v1.2.1 (2026-03-02 v2)
*   **✨ 新功能**：新增“分析报表”快捷入口，方便用户在上传/导入后快速切换回分析视图。
*   **🔄 优化**：增强 API 导入任务的持久化状态管理，切换页面或刷新后自动恢复任务进度。
*   **💄 UI**：更新首页功能引导步骤，明确“数据接入 -> 数据管理 -> 分析报告”的业务流程。

### v1.2.0 (2026-03-02 v1)
*   **✨ 新功能**：新增数据库文件管理界面 (`/api/files`)，支持查看和删除已下载的文件。
*   **🛠️ 优化**：优化 API 导入文件的命名格式为 `结构名称_月份_ID.xlsx`，方便识别和管理。
*   **🔧 工具**：提供 "批量重命名旧文件" 功能，可自动将旧格式文件名迁移到新格式。

### v1.1.1 (2026-03-01 v2)
*   **🔧 配置更新**：将默认部署端口更改为 `8888` 以适配阿里云环境。
*   **🐳 Docker**：更新 Dockerfile 暴露端口为 `8888`。
*   **🛠️ 开发**：更新 Vite 代理配置以匹配后端新端口。

### v1.1.0 (2026-03-01 v1)
*   **✨ 新功能**：添加自动版本管理机制。
*   **🐛 修复**：修复 Word 导出时图片被压缩变形的问题（动态计算宽高比）。
*   **💄 UI**：在界面右上角显示详细版本号和构建日期。

## ✨ 主要功能

*   **📊 数据上传与解析**：支持直接拖拽上传 Excel (`.xlsx`, `.xls`) 格式的传感器数据文件。
*   **🔌 API 批量导入 (后台任务)**：
    *   支持上传结构物列表 Excel (包含 ID, 名称, 类型)。
    *   **断点续传/去重**：自动识别已下载的月份数据，避免重复请求。
    *   **后台运行**：关闭网页后任务仍在服务器端继续运行。
    *   **一键重试**：对于网络波动导致的失败请求，支持单独一键重试。
*   **💾 数据持久化**：
    *   内置 SQLite 数据库，自动存储下载的 Excel 文件和生成的报告。
    *   支持历史数据查询和管理。
*   **📈 可视化分析**：自动解析数据并绘制高精度的时程曲线图，支持交互式缩放和查看具体数值。
*   **📝 智能统计**：自动计算每个传感器的关键指标，包括最大值、最小值、发生时间以及振幅/变化量。
*   **📑 报告导出**：一键生成包含图表和统计数据的专业 Word 报告（带页码和目录）。

## 🛠️ 技术栈

### 前端 (Frontend)
*   **框架**：[React 19](https://react.dev/) + [Vite](https://vitejs.dev/)
*   **样式**：[Tailwind CSS 4](https://tailwindcss.com/)
*   **图表**：[Recharts](https://recharts.org/)
*   **工具库**：`xlsx` (Excel解析), `docx` (Word生成), `date-fns` (日期处理)

### 后端 (Backend)
*   **运行时**：Node.js (v18+)
*   **Web框架**：Express.js
*   **数据库**：SQLite (`better-sqlite3`)
*   **功能**：API 代理、文件存储、后台任务队列

## 🚀 快速开始

### 本地开发 (Development)

1.  **安装依赖**：
    ```bash
    npm install
    ```

2.  **启动后端服务** (处理 API 请求和数据库)：
    ```bash
    node server/index.js
    ```
    *   服务运行在 `http://localhost:8888`

3.  **启动前端开发服务器** (另开终端)：
    ```bash
    npm run dev
    ```
    *   访问 `http://localhost:3000`

### 部署 (Deployment)

推荐使用 Docker 进行部署，包含完整的前后端环境。

1.  **构建并启动容器**：
    ```bash
    docker build -t structure-analytics .
    docker run -d -p 8888:8888 --name structure-app structure-analytics
    ```

2.  **访问应用**：
    打开浏览器访问 `http://<服务器IP>:8888`

### 阿里云部署建议

1.  在阿里云 ECS 上安装 Docker。
2.  上传代码或通过 Git 拉取。
3.  运行上述 Docker 命令。
4.  配置安全组规则，开放 `8888` 端口 (或通过 Nginx 反向代理到 80/443)。
5.  **数据持久化**：建议挂载宿主机目录以保存数据库和文件：
    ```bash
    docker run -d -p 8888:8888 \
      -v $(pwd)/storage:/app/storage \
      -v $(pwd)/database.sqlite:/app/database.sqlite \
      --name structure-app structure-analytics
    ```

## 📖 使用指南

1.  **API 导入**：
    *   准备结构物列表 Excel (包含 ID, 名称, 类型)。
    *   在 "API导入" 区域上传列表，选择月份，点击开始。
    *   系统将在后台下载数据，完成后点击右侧下载图标或直接在仪表盘查看。
2.  **手动上传**：直接拖拽传感器数据 Excel 到上传区域。
3.  **生成报告**：点击右上角 "导出 Word"，系统自动生成包含图表和统计的报告。

## 📄 许可证

Apache-2.0
